[ ] Configure cors in `api` for the frontend

[ ] Export error codes from api
[ ] See if we can generate the api endpoints from rails for ts - should be doable
[ ] Using the seed.rb files, make a script / rake task to generate .ts data files and then setup mocks
[ ] Full testing suite..
[ ] Add basic keyboard shortcuts to jump to pages (ctrl+1, ctrl+2)
[ ] Add terminal mode ctrl+` opens terminal view. 